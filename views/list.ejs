<% const isLogin = true %>

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Forum 메인</title>
    <link href="stylesheets/main.css" rel="stylesheet" />
    <link rel="stylesheet" href="/stylesheets/upload.css" />
  </head>
  <body>
    <div class="container">
      <!-- 상단바 -->
      <div class="nav">
        <div class="nav-title" onclick="location.href='/'">
          <img src="/images/running_icon.png" alt="메인아이콘" />
          <p>빽러닝</p>
        </div>
        <div class="nav-btns">
          <button onclick="location.href='/'">러닝코스</button>
          <button onclick="location.href='/post/talk'">러닝 톡</button>
          <button onclick="location.href='/auth/mypage'">내 프로필</button>
        </div>
      </div>

      <!-- 로그인 되었는지 확인하는 조건임!! -->
      <% if (isLogin) { %>

      <!-- 내용 -->
      <div class="content">
        <!-- 러닝코스 추가 버튼 -->
        <button class="btn mb-10" onclick="location.href='/write'">➕ 러닝코스 추가하기</button>

        <ul>
          <!-- 리스트 하나 -->
          <% write.forEach(function(item) { %>
          <li>
            <div class="box course-main-box" onclick="location.href='/list/<%= item._id%>' ">
              <img class="course-main-icon" src="images/course_icon.png" alt="코스아이콘" />
              <p class="course-main-title"><%= item.title %></p>
            </div>
          </li>
          <% }); %>
        </ul>
      </div>
      <% } else { %>
      <div class="content">
        <div class="no-login-notice">
          <p>😊</p>
          <p>현재 로그인을 하지 않았어요.</p>
          <p><span>내 프로필</span>에서 먼저 로그인을 하고 이용해주세요!</p>
        </div>
      </div>
      <% } %>
    </div>
  </body>
</html>
